import { NgxPipDirective } from './pip.directive';

describe('NgxPipDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxPipDirective();
        expect(directive).toBeTruthy();
    });
});

import { NgxVolumeDirective } from './volume.directive';

describe('NgxVolumeDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxVolumeDirective();
        expect(directive).toBeTruthy();
    });
});

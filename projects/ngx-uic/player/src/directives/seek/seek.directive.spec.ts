import { NgxSeekDirective } from './seek.directive';

describe('NgxSeekDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxSeekDirective();
        expect(directive).toBeTruthy();
    });
});

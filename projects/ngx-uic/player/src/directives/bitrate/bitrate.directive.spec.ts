import { NgxBitrateDirective } from './bitrate.directive';

describe('NgxBitrateDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxBitrateDirective();
        expect(directive).toBeTruthy();
    });
});

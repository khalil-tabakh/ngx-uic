import { NgxFullscreenDirective } from './fullscreen.directive';

describe('NgxFullscreenDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxFullscreenDirective();
        expect(directive).toBeTruthy();
    });
});

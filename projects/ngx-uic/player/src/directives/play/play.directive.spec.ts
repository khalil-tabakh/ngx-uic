import { NgxPlayDirective } from './play.directive';

describe('NgxPlayDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxPlayDirective();
        expect(directive).toBeTruthy();
    });
});

import { NgxResolutionDirective } from './resolution.directive';

describe('NgxResolutionDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxResolutionDirective();
        expect(directive).toBeTruthy();
    });
});

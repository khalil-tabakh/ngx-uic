import { NgxSpeedDirective } from './speed.directive';

describe('NgxSpeedDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxSpeedDirective();
        expect(directive).toBeTruthy();
    });
});

import { NgxLanguageDirective } from './language.directive';

describe('NgxLanguageDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxLanguageDirective();
        expect(directive).toBeTruthy();
    });
});

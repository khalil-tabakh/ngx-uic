import { NgxMuteDirective } from './mute.directive';

describe('NgxMuteDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxMuteDirective();
        expect(directive).toBeTruthy();
    });
});

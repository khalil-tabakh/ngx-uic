import { NgxLoopDirective } from './loop.directive';

describe('NgxLoopDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxLoopDirective();
        expect(directive).toBeTruthy();
    });
});

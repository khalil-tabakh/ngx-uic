import { NgxTimePipe } from './time.pipe';

describe('NgxTimePipe', () => {
    it('create an instance', () => {
        const pipe = new NgxTimePipe();
        expect(pipe).toBeTruthy();
    });
});

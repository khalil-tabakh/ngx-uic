import { NgxTriggerDirective } from './trigger.directive';

describe('NgxTriggerDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxTriggerDirective();
        expect(directive).toBeTruthy();
    });
});

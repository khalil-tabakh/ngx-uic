import { NgxPopupDirective } from './popup.directive';

describe('NgxPopupDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxPopupDirective();
        expect(directive).toBeTruthy();
    });
});

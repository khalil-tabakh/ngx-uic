import { NgxOptionDirective } from './option.directive';

describe('NgxOptionDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxOptionDirective();
        expect(directive).toBeTruthy();
    });
});
